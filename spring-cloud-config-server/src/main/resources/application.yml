spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mayerhorna/spring-cloud-config-server3
          search-paths: ConfigData
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8001